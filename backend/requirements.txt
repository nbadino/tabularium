fastapi>=0.141,<1
uvicorn[standard]>=0.52,<1
pydantic>=2.13,<3
pillow>=12,<13
numpy>=2,<3
requests>=2.32,<3
# Motore OCR dello pseudo-labeling (prefill «OCR (CPU)» e riempimento celle).
# Sta nelle dipendenze core perché è una funzione del prodotto, non un extra:
# senza, il prefill OCR risponde «nessun motore disponibile» e l'utente deve
# installare a mano qualcosa che l'app avrebbe dovuto consegnargli.
#
# `rapidocr` 3.x e non più `rapidocr-onnxruntime` 1.x: quest'ultimo dichiara
# `Requires-Python <3.13` e su 3.13 (la versione della CI macOS e di uno dei
# tre job Linux) non esiste nessuna release installabile — l'installazione si
# fermava lì. Il pacchetto nuovo non porta più il runtime né i modelli nel
# wheel: onnxruntime si installa a parte e i modelli si scaricano al primo
# uso, poi restano in cache. `services/ocr.py` parla entrambe le versioni,
# così un ambiente con la 1.x continua a funzionare.
rapidocr>=3,<4
onnxruntime>=1.17,<2
# Client delle API cloud (Vast.ai/RunPod) in services/cloud_manager.py.
httpx>=0.28,<1
python-dotenv>=1.0,<2
python-multipart>=0.0.20,<1
cryptography>=44,<46
# Download pesi dal registro modelli (model_registry.py)
huggingface-hub>=0.27,<1
# Ingestion PDF (split + anteprime): installare solo se serve
# pypdfium2>=2.0
