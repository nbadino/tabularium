-r requirements.txt
pytest>=9,<10
# Starlette 1.6 usa il fork compatibile httpx2 per TestClient.
httpx2>=2,<3
# Smoke E2E con browser reale (scripts/e2e_workflow.py).
selenium>=4,<5
# A runtime OpenCV non è più opzionale: il motore OCR (dipendenza core) porta
# `opencv-python`, e `pages.py` espone deskew/perspective solo se `cv2` si
# importa davvero. Non si installa anche `opencv-python-headless`: i due
# pacchetti scrivono lo stesso modulo `cv2` e averli entrambi rende
# indeterministico quale dei due vince. La build GUI non richiede un display
# per le operazioni che usiamo (soglia, warp, componenti connesse); su Linux
# richiede le librerie di sistema `libgl1` e `libglib2.0-0` — vedi README e CI.
